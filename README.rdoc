Amortization Schedule
--------------------------------------------------------------------------------------------------------------------------

A Rails Application for users to get the amortization schedule for their loan. Users have to input their requested loan amount, term(duration in months), interest rate and request date and the application will output the amortization schedule for the same.

Rspec is used for testing. To run the tests execute 'bundle exec rspec'.

